// Array.from
/*

The Array.from() method creates a new array instance from an array like or iterable object.

*/

const str = "Samid";


//It convert a string to an array of characters
const charArray = Array.from(str); //Array.from only accept strings
console.log(charArray);
