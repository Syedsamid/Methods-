// map

/*
The map method is used to trancform each element in an array and returns a new array
with the transformed elements. It's useful when you want to apply a function
to each element and store the result in a new array.

*/

// let number = [1, 2, 3, 4, 5];
// let doublenNum = number.map((number) => number * 4);

// console.log(doublenNum);

let numbers = [5, 4, 3, 2, 1];
// console.log(numbers.forEach((x) => x * x).filter((x) => x > 9)); //it will not return the value 
// console.log(numbers.map((x) => x * x).filter((x) => x > 9));

